<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>shina webpage</title>

  <!--  <script src = "arrayofobjects"></script>-->



</head>

<body>
<script>


var Data = {
  values: [
    {
principal: 2500,
time: 1.8
},
{
  principal:1000,
  time:5
},
{
  principal:3000,
  time:1
},
{
  principal:2000,
  time:3
},
  ]
}
//document.write (data.values[1].principal);
function interestCalculator ( principal, time ){
  var interestData = (principal * time)/100;
if (principal >= 2500 && time > 1 && time < 3){
var rate = 3 * interestData;
return rate

  //interestData = (principal * time)/100;



}
else if (principal >= 2500 && time >= 3){
  var rate = 4 * interestData;
  return rate;
  //interestData = (principal * rate * time)/100;



  }
  else if (principal < 2500 || time <= 1){
    var rate = 2 * interestData;
  return rate;
    //interestData =  (principal * rate * time)/100;



    }
else {
  var rate = 1 * interestData;


      return rate;
        //interestData = (principal * rate * time)/100;

}


return interestData;

}



//document.write ("interestCalculator is equal to " + interestData + "<br/>");

document.write(interestCalculator (5000, 3));


</script>

</body>


</html>
